import styled from 'styled-components';
import { FlexColumn } from '../styles/FlexBox';

export const ErrorBox = styled(FlexColumn)`
  margin: 50px;
  gap: 20px;
  align-items: center;
`;

export const ErrorLogo = styled.span`
  font-size: 10em;
`;
